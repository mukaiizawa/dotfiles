curl

# curl

    curl [options...] <url>

## options

     -i
         HTTP response headersを表示する。
     -I
         HTTP response headersのみ表示する。
     -s
         silent modeで実行する。
     -k
         セキュアでないサーバへの接続を許可する。
